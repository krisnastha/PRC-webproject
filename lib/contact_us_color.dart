import 'package:flutter/material.dart';

class ContactUsColors {
  static const Color root = Color(0xFF0076BD);      // Root: Blue
  static const Color primary = Color(0xFF0091D0);   // Primary: Light Blue
  static const Color secondary = Color(0xFF57595B); // Secondary: Gray
  static const Color text = Color(0xFF373435);      // Text: Dark Gray
}
